# Build locally
# Clone the repo && build
docker build . -t jenkinswithdocker

# Run
# Clone the repo && run
docker run -p 8080:8080 -v /var/run/docker.sock:/var/run/docker.sock --name jenkins bharadan24/jenkinswithdocker
